<p id="notice"><%= notice %></p>

<h1>Start page</h1>


<br/>
<pre> <%= link_to 'show_map', transferts_map_path %> </pre>
<br/>
<pre> <%= link_to 'transferts list', transferts_path %> </pre>
<br/>
<pre> <%= link_to 'reload transferts', reload_transferts_path, data: { confirm: 'Are you sure?' } %> </pre>
<br/>


<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    <% @starts.each do |start| %>
      <tr>
        <td><%= link_to 'Show', start %></td>
        <td><%= link_to 'Edit', edit_start_path(start) %></td>
        <td><%= link_to 'Destroy', start, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<!-- %= link_to 'New Start', new_start_path % -->
